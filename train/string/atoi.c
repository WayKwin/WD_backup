#include<stdio.h>
int  atoi (const char* str)
{
  if(str == NULL)
    return NULL;
  bool negetive_flag = *str == '-'? true:false;
  int count = 0;
  while(*str)
  {
    if(negetive_flag)
      continue;
  }

}
